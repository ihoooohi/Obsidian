## go和c++的不同

go中函数的只读的参数用值拷贝 
``
```go
func f(b Byteview) {}
```

c++中函数的只读的参数用常量引用 

```c++
void f(const Byteview & b) {}
```

所以go中的只读参数还是需要拷贝，而c++不用，go会有性能损失，但是这点复制的性能损失基本可以忽略
## 接收者&接口类型实现

方法的接收者分为**值接收者**和**指针接收者**

值接收者
```go
func (v Byteview) Len() int
```

指针接收者
```go
func (v *Byteview) Len() int
```

**在使用接口时，值接收者可以指针类型实现接口，指针接受者不能值类型实现接口**

|方法接收者|值类型实现接口|指针类型实现接口|
|---|---|---|
|值接收者|✅|✅|
|指针接收者|❌|✅|

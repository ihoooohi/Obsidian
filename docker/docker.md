
## Image & Container

👉 **容器是由镜像创建出来的**

`镜像  --->  docker run  --->  容器`

一张镜像可以创建 **多个容器**：

```bash
docker run nginx
docker run nginx
docker run nginx //这里的nginx是镜像名

```

每个都是**独立的容器**，互不影响。
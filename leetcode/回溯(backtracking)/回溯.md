# 核心本质

==回溯就是边构建边遍历二叉树==
回溯 = DFS + 撤销状态
有系统的暴力枚举

**for循环横向遍历，递归纵向深入**

回溯的精髓是**走错就退回**
## 回溯三要素

1. 路径（当前选了什么）
2. 选择列表（还能选什么）
3. 结束条件
## 经典回溯问题

- “所有可能”
- “全部排列”
- “全部组合”

## 核心框架
```go
func backtrack(状态...) {
    // 1) 结束条件：到达一个完整解
    if 满足结束条件 {
        记录答案（必要时 copy）
        return
    }

    // 2) 遍历选择：本层有哪些可选项
    for 每个 choice {
        // 3) 剪枝 / 约束：不合法就跳过
        if 不合法(choice, 状态) { continue }

        // 4) 做选择：修改状态（path/used/open/close...）
        apply(choice)

        // 5) 递归：去下一层
        backtrack(新状态...)

        // 6) 撤销选择：恢复状态（回撤）
        undo(choice)
    }
}
```
==用continue剪枝==
## 回溯题目

### 46.全排列（排列）


![[6ef453a539c5283061456a4fb3116408.jpg]]

比如说`[1,2,3]`的全排列，先是 path `[1,2,3]`走到尽头，然后触发满足条件 return，返回上一层，
上一层是深度2，是取3，path`[1,2,3]`，运行backtrack后的代码，移除选择，path`[1,2]`, 然后for循环继续查看2后面还有没有除了3的选择，发现没有，for结束，继续返回上一层
上一层是深度1，是取2，path`[1,2]`，运行backtrack后的代码，移除选择，path`[1]`, 然后for循环继续查看1后面还有没有除了2的选择，发现有3，于是继续走`[1,3]`的路

### 22.括号生成(括号规则)

括号规则[[括号]]

不合法状态是左括号数量大于n，以及当前右括号数量大于左括号，需要将其剪枝

用open记录左括号数量，close记录右括号数量

```go
func generateParenthesis(n int) []string {
    choice := []byte{'(', ')'}
    path := make([]byte, 0)
    res := make([]string,0)
    backtrack(n, 0, 0, path, choice, &res)
    return res
} 

func backtrack(n, open, close int, path []byte, choice []byte, res *[]string){
    //end traverse
    if open == n && close == n{
        *res = append(*res, string(path))
        return
    }
    //choice loop
    for _, ch := range choice {
        if ch == '('{
            if open >= n{continue}
        } else{
            if close >= open{continue}
        }
    //add path
        if ch == '('{
            open += 1
        }else{
            close += 1
        }
        path = append(path, ch)
    //backtrack
        backtrack(n, open, close, path, choice, res)
    //remove path
        path = path[:len(path)-1]
        if ch == '('{
            open -= 1
        }else{
            close -= 1
        }
    }
}
```
## go语言的陷阱

将path数组加进res二维数组时，不能直接
```go
*res = append(*res, path)
```
因为path这个slice底层是指针，之前加进去的值会随着path改动而改动，可能最后res的值会是都一样的
```text
[[3 2 1]
 [3 2 1]
 [3 2 1]
 ...]
```
必须每次先copy一份再加进去
```go
tmp := make([]int, len(nums))
copy(tmp, path)
*res = append(*res, tmp)
```
这样每次都是新数组，新内存，不共享底层数据

c++就没这个困扰，vector 的拷贝构造函数执行深一层复制，**深拷贝**，所以 push_back(path) 会复制元素，不共享底层数组
```c++
res.push_back(path);
```

==浅拷贝：指向同一个内存  
深拷贝：指向两个不同的内存==
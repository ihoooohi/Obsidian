## 23.合并k个升序链表（分治递归）

首先要会合并两个升序链表
```go
func mergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {
    dummy := &ListNode{}
    cur := dummy

    for list1 != nil && list2 != nil {
        if list1.Val < list2.Val {
            cur.Next = list1
            list1 = list1.Next
        } else {
            cur.Next = list2
            list2 = list2.Next
        }
        cur = cur.Next
    }

    if list1 == nil {
        cur.Next = list2
    }else{
        cur.Next = list1
    }
    return dummy.Next
}
```

如果一个个合并k个链表的话，时间复杂度来到了O（nk），很大了，要降低时间复杂度，可用分支思想，每次两两一起合并，8条-4条-2条-1条，所以时间复杂度为O（nlogk）

每次切半，两块区域一起合并

比如有 4 个链表：
```text
mergeRange(0,3)
         /      \
   mergeRange  mergeRange
     (0,1)        (2,3)
     /   \        /   \
   L0    L1     L2    L3
```
先把底层两两合并，再往上合并。

**那么怎么用代码描述这个递归树呢？递归相关代码怎么写呢？**

![[116a0d70fa723d3c2155694c726d41d9.jpg]]


```go
func mergeRange(lists []*ListNode, left int, right int) *ListNode {
    if left == right {
        return lists[left]
    }

    mid := left + (right - left)/2
    l1 := mergeRange(lists, left, mid)
    l2 := mergeRange(lists, mid+1, right)
    
    return mergeTwoLists(l1, l2)
}
```

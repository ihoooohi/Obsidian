GopherAI是一个ai对话平台（类似于chatgpt），有登录功能

## 登录功能

符合mvc分层架构
用户前端点击登录，路由层router监听到/login路径，controller收到post请求，调用service查询用户，校验密码，与DAO层数据库比对，如果校验通过就生成jwt返回

关于jwt
jwt存在用户浏览器或app中，每次新的请求，header都会带上jwt这样就不用验证了
与之对比的session则存在服务器中，存在时间比jwt久

